# Special "Content" Files

The publishing engine uses filenames as headers. Sometimes this is undesirable. Users want to use special naming - filename containing a special marker - to treat file's content directly, not as a separated section.

- default marker is `_contents_`
- the marker shall be a configurable publish config option (`contents_marker`).
