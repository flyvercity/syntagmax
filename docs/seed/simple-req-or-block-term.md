# Simple Requirement or Text Block Terminator

In the current implementation, with Obsidian driver, marked non-artifact text blocks and artifact blocks (e.g. requirements) terminate with `[/TAG]` syntax (e.g., `[/COM]` or `[/REQ]`).

The users insist that such block should also be terminated with one or more empty line.
