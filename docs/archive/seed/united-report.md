Combine all outputs into a single Markdown-based report:
- unite errors, metrics, impact, and AI-analysis report
- rename `--error-output` flag appropriately
- if `--render-tree` is given, add the tree into the same report instead on console.
- use `<projet-working-dir>/.syntagmax/reports/report.md` as default
